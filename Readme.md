# Agent Llama Index

## Getting Started

### Installation

Install dependencies using poetry:

```bash
poetry install
```

### Running the server

Run the server using uvicorn:

```bash
uvicorn main:app --host 0.0.0.0 --port 5000 --reload True